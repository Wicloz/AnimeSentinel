@if(isset($mal_url))
  <div class="content-generic flowfix">
    <div class="malw-sidebar">
      <iframe src="{{ $mal_url }}"></iframe>
    </div>
    <div class="content-close"></div>
  </div>
@endif
