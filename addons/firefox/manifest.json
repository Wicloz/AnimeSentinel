{
  "manifest_version": 2,
  "name": "AnimeSentinel Companion AddOn",
  "version": "0.5",

  "description": "Companion AddOn for AnimeSentinel. Current features: (1) Allows certain blocked videos to be watched. (2) Will make it easier to watch videos behind the Cloudflare filter. (3) Tells the website it is installed to which it will react accordingly.",

  "permissions": [
    "webRequest",
    "webRequestBlocking",
    "<all_urls>"
  ],

  "background": {
    "scripts": ["modifyHeaders.js"]
  },

  "content_scripts": [{
    "matches": [
      "*://development.wilcodeboer.me/*",
      "*://anime.wilcodeboer.me/*"
    ],
    "js": ["notifyEnabled.js"]
  }]
}
